export const environments = { //aqui definimos todas las variables de entorno que necesitamos
  baseUrl: 'http://localhost:3000'//en este caso en particular, esta baseUrl hace referencia a la base de estas url http://localhost:3000/users y http://localhost:3000/heroes que son las url que nos proporciona el servidor json-server y que contienen la informacion de nuestro backend que nos hemos creado dentro de la carpeta data en el archivo db.json
  }
